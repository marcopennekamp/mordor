# This program tests all possible add combinations and checks their results.


function test.add_long (i64 a, i64 b) i64
    load    a
    load    b
    add
    return

function test.add_int (i32 a, i32 b) i32
    load    a
    load    b
    add
    return
